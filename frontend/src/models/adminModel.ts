export interface Admin {
  id: number;
  sapid: number;
  name: string;
  email: string;
  department: string;
  contactNumber: number;
  userId: number;
  createdAt?: string;
  updatedAt?: string;
}
