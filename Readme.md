in the second commit .. me as a team leader install all required and needed package in backend. creata project backend structure & connectg the database, and create a one table in it. 

to create a jwt token secret run this command

      | node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"